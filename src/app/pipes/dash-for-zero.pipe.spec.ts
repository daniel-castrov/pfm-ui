import { DashForZeroPipe } from './dash-for-zero.pipe';

describe('DashForZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new DashForZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
