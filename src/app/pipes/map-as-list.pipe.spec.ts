import { MapAsListPipe } from './map-as-list.pipe';

describe('MapAsListPipe', () => {
  it('create an instance', () => {
    const pipe = new MapAsListPipe();
    expect(pipe).toBeTruthy();
  });
});
